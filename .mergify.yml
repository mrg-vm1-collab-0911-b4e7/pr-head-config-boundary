scopes:
  source:
    files:
      docs:
        include:
          - docs/**/*
      security:
        include:
          - security/**/*

queue_rules:
  - name: default
    batch_size: 2
    queue_conditions:
      - check-success = vm1-scope-hold
